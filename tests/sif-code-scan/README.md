# sif-code-scan tester

## Kjør lokalt

```sh
cd .github/actions/sif-code-scan && npm ci && npm test
```

## Struktur

- `scan.test.ts` — vitest-tester for `scan.ts`
- `fixtures/` — ferdiglagde xlsx/docx-filer brukt av testene
