name: Build app
on:
  workflow_call:
    inputs:
      runs-on:
        required: false
        type: string
        description: Miljø versjon som skal brukes til bygg.
        default: 'ubuntu-latest'
      t-2c:
        required: false
        type: string
        description: Tråd konfig til maven.
        default: ''
      skip-tests:
        type: boolean
        required: false
        description: 'Skip tests'
        default: false
      java-version:
        required: false
        type: string
        description: Java SDK versjon som skal brukes.
        default: '21'
      sonar-scan:
        required: false
        type: boolean
        description: Skal sonar scan brukes?
        default: false
      build-image:
        required: false
        type: boolean
        description: Skal docker image bygges?
        default: true
      push-image:
        required: false
        type: boolean
        description: Skal docker image pushes?
        default: false
      upload-image:
        required: false
        type: boolean
        description: Skal docker image lastes opp til jobben?
        default: false
      additional-tag:
        required: false
        type: string
        description: Ekstra label å tagge docker image med - nyttig for repoer med dev brancher som pushes til dev
      namespace:
        required: false
        type: string
        description: "NAV team"
        default: "k9saksbehandling"
      quiet:
        required: false
        type: boolean
        description: 'Maven quiet mode'
        default: true
      clean-before-caching:
        required: false
        type: boolean
        description: 'Clean up generated stuff before caching'
        default: true
    outputs:
      build-version:
        description: "Build version"
        value: ${{ jobs.build-and-test.outputs.build-version }}
      image-artifact-name:
        description: 'local docker image artifact name to be downloaded '
        value: ${{ jobs.build-and-test.outputs.image-artifact-name }}
jobs:
  build-and-test:
    name: Build and test
    permissions:
      contents: read
      id-token: write
    runs-on: ${{ (github.actor == 'dependabot[bot]' && 'ubuntu-latest') || inputs.runs-on }}

    env:
      TZ: "Europe/Oslo"

    outputs:
      build-version: ${{ steps.build-and-test.outputs.build-version }}
      image-artifact-name: ${{ steps.docker-build.outputs.image-artifact-name }}

    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # ratchet:actions/checkout@v4
        with:
          fetch-depth: 0 # checkout gir shallow copy - fetch-depth: 0 vil tilgjengeliggjøre tags
      - name: Login to GitHub Packages Docker Registry
        uses: docker/login-action@5e57cd118135c172c3672efd75eb46360885c0ef # ratchet:docker/login-action@v3.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.READER_TOKEN }}
      - name: Setup java and maven
        uses: navikt/sif-gha-workflows/.github/actions/maven/setup-java-and-maven@main # ratchet:exclude
        with:
          github-token: ${{ secrets.READER_TOKEN }} # bruker READER_TOKEN pga private repos
          java-version: ${{ inputs.java-version }}
      - name: Maven install and test
        id: build-and-test
        uses: navikt/sif-gha-workflows/.github/actions/maven/test-and-build@main # ratchet:exclude
        with:
          t-2c: ${{ inputs.t-2c }}
          skip-tests: ${{ inputs.skip-tests }}
          quiet: ${{ inputs.quiet }}
      - name: Bygg og push docker image
        id: docker-build
        if: inputs.build-image
        uses: navikt/sif-gha-workflows/.github/actions/maven/build-push-docker-image@main # ratchet:exclude
        with:
          build-version: ${{ steps.build-and-test.outputs.build-version }}
          push-image: ${{ inputs.push-image }}
          upload-image: ${{ inputs.upload-image }}
          additional-tag: ${{ inputs.additional-tag }}
          docker_platforms: ${{ (inputs.upload-image && 'linux/amd64') || null }} # docker image tarball støtter bare én platform av gangen
          namespace: ${{ inputs.namespace }}
      - name: Clean up generated stuff before caching
        if: inputs.clean-before-caching
        # målet er å ha mest mulig avhengigheter i cachen, uten å få med moduler som uansett bygges av i repositoriet neste gang
        # sletter alt under i m2-repo under no/nav/k9 bortsett fra felles og prosesstask, samt no/nav/ung
        # sletter under no/nav/folketrygdloven siden k9-sak og ft-kalkulus bygger moduler her. Unntar bregning-moduler da disse importers
        run: |
          find ~/.m2/repository/no/nav/k9/ -mindepth 1 -maxdepth 1 ! -name felles ! -name prosesstask -exec rm -r {} +
          find ~/.m2/repository/no/nav/folketrygdloven/ -mindepth 1 -maxdepth 1 ! -name beregning* ! -name prosesstask -exec rm -r {} +
          rm -r ~/.m2/repository/no/nav/ung/sak