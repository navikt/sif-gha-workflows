name: SIF Code Scan
description: Scans repository for sensitive data like Norwegian national identity numbers (fødselsnummer). Only FNR from the bundled allowed-fnr lists are allowed.

runs:
  using: "composite"
  steps:
    - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # ratchet:actions/setup-node@v7.0.0
      with:
        node-version: 24
    - uses: pnpm/action-setup@ea17c68df8912ef543352723c149a84f56e3d413 # ratchet:pnpm/action-setup@v6.1.0
      with:
        version: 12.3.4
    - name: Scan for sensitive data
      shell: bash
      run: |
        pnpm --dir "${{ github.action_path }}" install --frozen-lockfile --prod --ignore-scripts
        node "${{ github.action_path }}/scan.ts"
